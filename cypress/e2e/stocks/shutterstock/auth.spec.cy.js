import { loginToShutterstock } from '../../../support/shutterstock/auth';

describe('Shutterstock Login Test', () => {
    it('Log in to Shutterstock', () => {
        loginToShutterstock();
    });
});
